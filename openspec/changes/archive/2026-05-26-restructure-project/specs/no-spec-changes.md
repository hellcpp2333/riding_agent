## Notes

此变更为纯项目结构重构，不涉及任何功能性需求变更。

所有 API 接口行为保持不变：
- `/` - 返回前端页面
- `/api/sessions` - 会话列表
- `/api/sessions` (POST) - 新建会话
- `/api/chat` - 对话（SSE 流式）
- `/api/route/plan` - 结构化路线规划

无 ADDED/MODIFIED/REMOVED Requirements。