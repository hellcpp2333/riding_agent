## 1. HTML 模板

- [x] 1.1 在 `#messages` 容器内添加欢迎语 DOM 结构，使用 `v-if="messages.length === 0"` 控制显示

## 2. 样式

- [x] 2.1 在 `style.css` 中添加欢迎语卡片样式（居中布局、emoji 图标、功能列表项样式），复用 earth-tone 设计变量

## 3. 验证

- [x] 3.1 验证新会话显示欢迎语，发送消息后欢迎语消失
- [x] 3.2 验证切换会话时欢迎语正确显示/隐藏
