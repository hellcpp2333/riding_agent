## 1. 样式调整

- [ ] 1.1 在 `renderDistanceMarkers()` 中将圆点尺寸从 `8px` 改为 `14px`
- [ ] 1.2 将 box-shadow 从 `0 0 0 2px rgba(255,255,255,0.7)` 改为 `0 0 0 3px rgba(255,255,255,0.8)`
- [ ] 1.3 将文字字号从 `11px` 改为 `12px`，padding 从 `1px 5px` 改为 `2px 6px`

## 2. 验证

- [ ] 2.1 规划一条路线，验证标记圆点明显盖过路线宽度，视觉效果醒目
